#pragma once

#include <string>


/** generate a random, printable string with count characters
 * @param count the number of characters
 * @return the random string
 */
extern std::string generateRandomString(int count);
